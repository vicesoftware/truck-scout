-- Initialize test database
-- This script ensures the test database is set up correctly

-- No additional setup needed as the database is created by environment variables
-- in docker-compose.test.yml (POSTGRES_DB: tms_test_db)
